name: Shell Commands

on: [push]
 schedule: 
   - cron: "0/1 * * * *"
jobs: 
   runs-on: windows-latest
           
   steps:
     - name: echo a string
       run: echo "Hello"
     - name: multiline script
       run: |
        ls
        node -v
        npm -v
    